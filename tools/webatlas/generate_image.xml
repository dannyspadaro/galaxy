<tool id="webatlas_generate_image" name="Webatlas Generate image" version="@TOOL_VERSION@+galaxy@VERSION_SUFFIX@" python_template_version="3.5" profile="@PROFILE_VERSION@">
    <description>TODO description</description>
    <macros>
        <import>macros.xml</import>
    </macros>
    <expand macro="requirements"/>
    <command detect_errors="exit_code"><![CDATA[
    #if str( $input_type.input_type_selector ) == "label_visium":
        ## #if $input_type.input_visium.ext = 'h5ad':
            ln -s '$input_visium' input.h5ad &&
            python "${__tool_directory__}/generate_image.py" ${prefix} label visium input.h5ad
        ## #elif $input_type.input_visium.ext = 'gz':
        ##     mkdir unpacked_spaceranger &&
        ##     tar -xf '${input_visium}' -C unpacked_spaceranger &&
        ##     python "${__tool_directory__}/generate_image.py" ${prefix} label visium unpacked_spaceranger
        ## #end if
    ## #elif str( $input_type.input_type_selector ) == "label_xenium":
    ##     #if $input_type.input_xenium.format = 'gz':
    ##         mkdir unpacked_xenium &&
    ##         tar -xf '${input_xenium}' -C unpacked_xenium &&
    ##         python "${__tool_directory__}/generate_image.py" ${prefix} label xenium unpacked_xenium
    ##     #elif $input_type.input_xenium.format = 'zip':
    ##         python "${__tool_directory__}/generate_image.py" ${prefix} label xenium '${input_xenium}'
    ##     #end if
    ## #elif str( $input_type.input_type_selector ) == "label_merscope":
    ##     mkdir unpacked_merscope &&
    ##     tar -xf '${input_merscope_label}' -C unpacked_merscope &&
    ##     python "${__tool_directory__}/generate_image.py" ${prefix} label merscope unpacked_merscope
    ## #elif str( $input_type.input_type_selector ) == "raw_merscope":
    ##     mkdir unpacked_merscope &&
    ##     tar -xf '${input_merscope_raw}' -C unpacked_merscope &&
    ##     python "${__tool_directory__}/generate_image.py" ${prefix} raw merscope unpacked_merscope
    #end if
    ]]></command>
    <inputs>
        <conditional name="input_type">
            <param name="input_type_selector" type="select" label="Choose the type of input data" help="">
                <option value="label_visium">Visium (spaceranger)</option>
                <option value="label_xenium">Xenium</option>
                <option value="label_merscope">Merscope data with 'cell_boundaries'</option>
                <option value="raw_merscope">Merscope data with raw tif files</option>
            </param>
            <when value="label_visium">
                <param name="input_visium" type="data" format="h5ad" label="h5ad file or compressed Spaceranger output"/>
            </when>
            <when value="label_xenium">
                <param name="input_xenium" type="data" format="gz,zip" label="Compressed Xenium output or cells.zarr.zip file"/>
            </when>
            <when value="label_merscope">
                <param name="input_merscope_label" type="data" format="gz" label="Compressed MERSCOPE output"/>
            </when>
            <when value="raw_merscope">
                <param name="input_merscope_raw" type="data" format="gz" label="Compressed MERSCOPE output"/>
            </when>
        </conditional>
        <param name="prefix" type="text" label="Enter a prefix for the output file(s)" optional="false" default_value="prefix"/>
    </inputs>
    <outputs>
        <data name="output_label" format="tif" label="${tool.name} on ${on_string}: label tif" from_work_dir="prefix-label.tif">
            <filter>input_type['input_type_selector'] == 'label_visium' or input_type['input_type_selector'] == 'label_xenium' or input_type['input_type_selector'] == 'label_merscope'
            </filter>
        </data>
        <data name="output_raw" format="tif" label="${tool.name} on ${on_string}: raw tif" from_work_dir="prefix-raw.tif">
            <filter>input_type['input_type_selector'] == 'raw_merscope'</filter>
        </data>
    </outputs>
    <tests>
        <!-- Hint: You can use [ctrl+alt+t] after defining the inputs/outputs to auto-scaffold some basic test cases. -->
    </tests>
    <help><![CDATA[

.. class:: infomark

**What it does**

TODO: Fill in help in reStructuredText format (https://docutils.sourceforge.io/docs/ref/rst/restructuredtext.html)

Usage
.....


**Input**


**Output**


    ]]></help>
    <expand macro="citations"/>
</tool>