<tool id="webatlas_ome_zarr_metadata" name="Webatlas OME Zarr Metadata" version="@TOOL_VERSION@+galaxy@VERSION_SUFFIX@" profile="@PROFILE_VERSION@">
    <description>TODO description</description>
    <macros>
        <import>macros.xml</import>
    </macros>
    <expand macro="requirements"/>
    <command detect_errors="exit_code"><![CDATA[
    #if $input_type_selector == "zarr":
        python "${__tool_directory__}/ome_zarr_metadata.py" '$input_zarr.extra_files_path/$input_zarr.metadata.store_root/OME/METADATA.ome.xml' > output.json
    ##remove zarr.zip?
    #elif $input_type_selector == "zarr.zip":
        mkdir unpacked_zarr &&
        unzip $input_zarr_zip -d unpacked_zarr &&
        python "${__tool_directory__}/ome_zarr_metadata.py" unpacked_zarr/output.zarr/OME/METADATA.ome.xml > output.json
    ##remove xml input too?
    #elif $input_type_selector == "xml":
        ln -s '$input_xml' input.xml &&
        python "${__tool_directory__}/ome_zarr_metadata.py" input.xml > output.json
    #end if
    ]]></command>
    <inputs>
        <conditional name="input_type">
            <param name="input_type_selector" type="select" label="Choose the type of input data">
                <option value="zarr">zarr</option>
                <option value="zarr.zip">zarr.zip</option>
                <option value="xml">xml</option>
            </param>
            <when value="zarr">
                <param name="input_zarr" type="data" format="zarr" label="zarr directory"/>
            </when>
            <when value="zarr.zip">
                <param name="input_zarr_zip" type="data" format="zarr.zip" label="zipped zarr directory"/>
            </when>
            <when value="xml">
                <param name="input_xml" type="data" format="xml" label="xml file"/>
            </when>
        </conditional>
    </inputs>
    <outputs>
        <data name="output_json" format="json" label="${tool.name} on ${on_string}" from_work_dir="output.json"/>
    </outputs>
    <tests>
        <!-- Hint: You can use [ctrl+alt+t] after defining the inputs/outputs to auto-scaffold some basic test cases. -->
    </tests>
    <help><![CDATA[

.. class:: infomark

**What it does**
Function that parses an OME XML file and dumps basic metadata as a JSON formatted string.
Takes an OME XML file as input or a zarr directory that was created by 'Webatlas Consolidata Metadata'.

TODO: Fill in help in reStructuredText format (https://docutils.sourceforge.io/docs/ref/rst/restructuredtext.html)

Usage
.....


**Input**


**Output**


    ]]></help>
    <expand macro="citations"/>
</tool>