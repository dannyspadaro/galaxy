<tool id="webatlas_write_spatialdata" name="Webatlas Write Spatialdata" version="@TOOL_VERSION@+galaxy@VERSION_SUFFIX@" python_template_version="3.5" profile="@PROFILE_VERSION@">
    <description>TODO description</description>
    <macros>
        <import>macros.xml</import>
    </macros>
    <requirements>
        <requirement type="package" version="0.5.0">fire</requirement>
        <requirement type="package" version="2023.9.26">tifffile</requirement>
        <requirement type="package" version="0.9.1">anndata</requirement>
        <requirement type="package" version="0.15.0">rioxarray</requirement>
        <requirement type="package" version="0.0.15">spatialdata</requirement>
    </requirements>
    <command detect_errors="exit_code"><![CDATA[
        #if $input_type_selector == "h5ad":
            ln -s '$input_h5ad' input.h5ad &&
            ##ln -s '$input_raw_img' input_raw.tif &&
            ln -s '$input_label_img' input_label.tif &&
            python "${__tool_directory__}/write_spatialdata.py" input.h5ad $prefix --label_img_path input_label.tif
        #elif $input_type_selector == "zarr":
            ln -s '$input_zarr' input.zarr &&
            ln -s '$input_raw_img' input_raw.tif &&
            ln -s '$input_label_img' input_label.tif &&
            python "${__tool_directory__}/write_spatialdata.py" input.zarr $prefix --raw_img_path input_raw.tif --label_img_path input_label.tif
        #end if
    ]]></command>
    <inputs>
        <conditional name="input_type">
            <param name="input_type_selector" type="select" label="Choose the type of input data">
                <option value="h5ad">h5ad</option>
                <option value="zarr">zarr</option>
            </param>
            <when value="h5ad">
                <param name="input_h5ad" type="data" format="h5ad" label="h5ad file" help="Output generated by 'Webatlas Process files'"/>
            </when>
            <when value="zarr">
                <param name="input_zarr" type="data" format="zarr" label="zarr directory" help="Output generated by 'Webatlas Process files'"/>
            </when>
        </conditional>
        <param name="input_raw_img" type="data" format="tiff" label="raw tif images" optional="true" multiple="true"/>
        <param name="input_label_img" type="data" format="tiff" label="labeled tif images" optional="true" multiple="true"/>
        <param name="prefix" type="text" label="Enter a prefix for the output file(s)" optional="false" value="prefix"/>
    </inputs>
    <outputs>
        <data name="output_zarr" format="zip" label="${tool.name} on ${on_string}: zarr output" from_work_dir="output.zarr.zip"/>
    </outputs>
    <tests>
        <!-- Hint: You can use [ctrl+alt+t] after defining the inputs/outputs to auto-scaffold some basic test cases. -->
    </tests>
    <help><![CDATA[

.. class:: infomark

**What it does**

TODO: Fill in help in reStructuredText format (https://docutils.sourceforge.io/docs/ref/rst/restructuredtext.html)

Usage
.....


**Input**


**Output**


    ]]></help>
    <expand macro="citations"/>
</tool>