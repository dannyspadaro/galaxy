<tool id="webatlas_build_config" name="Webatlas Build config" version="@TOOL_VERSION@+galaxy@VERSION_SUFFIX@" python_template_version="3.5" profile="@PROFILE_VERSION@">
    <description>TODO description</description>
    <macros>
        <import>macros.xml</import>
    </macros>
    <expand macro="build_config_requirements"/>
    <command detect_errors="exit_code"><![CDATA[    
        ##pip install vitessce==3.1.0 &&
        ln -s "$input_zarr" input.zarr &&
        ln -s "$input_img_1" input_img_1.zarr &&
        ln -s "$input_img_metadata_1" input_img_metadata_1.json &&
        python "${__tool_directory__}/build_config.py" --file_paths input.zarr --images '{"label": [{"path": "input_img_1.zarr", "md": "input_img_metadata_1.json"}]}'
    ]]></command>
    <inputs>
        <param name="input_zarr" type="data" format="zarr" label="Zarr file generated by 'Webatlas Process files'"/>
        <section name="input_section_1" title="Image file and corresponding metadata file">
            <param name="input_img_1" type="data" format="zarr" label="Zarr file generated by 'Webatlas Consolidate Metadata'"/>
            <param name="input_img_metadata_1" type="data" format="json" label="JSON file generated by 'Webatlas OME Zarr Metadata'"/>
        </section>
    </inputs>
    <outputs>
        <data name="output_json" format="json" label="${tool.name} on ${on_string}" from_work_dir="--config.json"/>
    </outputs>
    <tests>
        <!-- Hint: You can use [ctrl+alt+t] after defining the inputs/outputs to auto-scaffold some basic test cases. -->
    </tests>
    <help><![CDATA[

.. class:: infomark

**What it does**

TODO: Fill in help in reStructuredText format (https://docutils.sourceforge.io/docs/ref/rst/restructuredtext.html)

Usage
.....


**Input**


**Output**


    ]]></help>
    <expand macro="citations"/>
</tool>