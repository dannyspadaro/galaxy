runners:
  local:
    load: galaxy.jobs.runners.local:LocalJobRunner
    workers: 4

handling:
  processes:
    handler0:

execution:
  default: docker_local
  environments:
    local:
      runner: local
    docker_local:
      runner: local
      docker_enabled: true
      docker_volumes: "$defaults"
      docker_auto_rm: true